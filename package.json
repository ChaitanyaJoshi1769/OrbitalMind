{
  "name": "orbitalmind",
  "version": "0.1.0",
  "description": "The AI Inference Infrastructure Layer for Space",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'Build infrastructure ready'",
    "dev": "echo 'Development setup ready'",
    "test": "echo 'Testing infrastructure ready'",
    "clean": "find . -type d -name 'dist' -o -name 'node_modules' | xargs rm -rf"
  }
}
